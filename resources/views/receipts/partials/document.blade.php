<div class="flex justify-between"><div><h2 class="text-2xl font-black">EDUTECH</h2><p>Comprobante de pago</p></div><div class="text-right"><strong>{{ $receipt->number }}</strong><p>{{ $receipt->issued_on?->format('d/m/Y') ?? 'Fecha histórica no confiable' }}</p></div></div><hr class="my-6"><p><strong>Estudiante:</strong> {{ $receipt->student_name }}</p><p><strong>Curso / matrícula:</strong> {{ $receipt->course_name }}</p><table><tr><td>Saldo anterior</td><td class="text-right">{{ $receipt->previous_balance === null ? 'No calculable' : '$'.number_format($receipt->previous_balance,0,',','.') }}</td></tr><tr><td>Pago ({{ $receipt->payment_method }})</td><td class="text-right">-${{ number_format($receipt->payment_amount,0,',','.') }}</td></tr><tr><td><strong>Nuevo saldo</strong></td><td class="text-right"><strong>{{ $receipt->new_balance === null ? 'No calculable' : '$'.number_format($receipt->new_balance,0,',','.') }}</strong></td></tr></table>@if($receipt->legacy_reference)<p class="mt-4 text-sm">Referencia histórica: {{ $receipt->legacy_reference }}</p>@endif<p class="mt-8 text-sm">Cajero: {{ $receipt->cashier_name }}</p>
