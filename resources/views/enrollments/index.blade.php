@extends('layouts.app', ['title'=>'Matrículas'])
@section('content')
<div class="mb-7 flex items-end justify-between"><div><h2 class="text-2xl font-bold">Matrículas</h2><p class="mt-1 text-slate-500">Historial académico de estudiantes.</p></div><a href="{{ route('enrollments.create') }}" class="btn-primary">+ Nueva matrícula</a></div>
<div class="card overflow-x-auto"><table><thead><tr><th>Estudiante</th><th>Programa / curso</th><th>Grupo</th><th>Inicio cobro</th><th>Valor acordado</th><th>Estado</th></tr></thead><tbody>@forelse($enrollments as $item)<tr><td><a class="font-semibold text-slate-900 hover:text-teal-700" href="{{ route('students.show',$item->student) }}">{{ $item->student->full_name }}</a><p class="text-xs text-slate-400">{{ $item->student->document_number }}</p></td><td>{{ $item->group->course->name }}</td><td>{{ $item->group->name }}</td><td>{{ $item->billing_start_date?->format('d/m/Y') ?? 'Sin fecha' }}</td><td class="font-semibold">${{ number_format($item->agreed_amount,0,',','.') }}</td><td><span class="badge">{{ $item->status->label() }}</span></td></tr>@empty<tr><td colspan="6" class="py-12 text-center text-slate-400">Aún no hay matrículas.</td></tr>@endforelse</tbody></table><div class="mt-6">{{ $enrollments->links() }}</div></div>
@endsection
