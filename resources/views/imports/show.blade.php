@extends('layouts.app', ['title' => 'Previsualización de importación'])
@section('content')<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach($import->summary as $key=>$value)<div class="card p-5"><div class="text-xs uppercase text-slate-500">{{ str_replace('_',' ',$key) }}</div><div class="text-3xl font-black">{{ $value }}</div></div>@endforeach</div><div class="card mt-6 p-6"><h2 class="font-black">Mapeo detectado</h2><pre class="mt-3 overflow-auto rounded bg-slate-950 p-4 text-xs text-slate-100">{{ json_encode($import->mapping, JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre>@if($import->status!=='confirmed')<form class="mt-5" method="POST" action="{{ route('imports.confirm',$import) }}">@csrf<button class="btn-primary">Confirmar importación definitiva</button></form>@else<p class="mt-5 font-bold text-emerald-700">Importación confirmada e idempotente.</p>@endif</div><div class="card mt-6 overflow-x-auto"><table class="w-full text-sm"><thead><tr><th>Fila</th><th>Estudiante</th><th>Estado</th><th>Inconsistencias</th></tr></thead><tbody>@foreach($import->rows->take(100) as $row)<tr><td>{{ $row->source_row }}</td><td>{{ $row->normalized_data['name'] }}</td><td>{{ $row->status }}</td><td>{{ implode(', ',$row->inconsistencies ?? []) }}</td></tr>@endforeach</tbody></table></div>@endsection
