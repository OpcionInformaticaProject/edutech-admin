<div><div class="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 class="text-2xl font-bold">Directorio de estudiantes</h2><p class="mt-1 text-slate-500">Consulta y administra la información académica.</p></div><a href="{{ route('students.create') }}" class="btn-primary">+ Nuevo estudiante</a></div>
<div class="card"><div class="mb-6 flex flex-col gap-3 sm:flex-row"><div class="relative flex-1"><input wire:model.live.debounce.300ms="search" class="input pl-10" placeholder="Buscar por nombre o documento…"><span class="absolute left-3 top-2.5 text-slate-400">⌕</span></div><select wire:model.live="status" class="input sm:w-48"><option value="all">Todos los estados</option><option value="active">Activos</option><option value="inactive">Inactivos</option></select></div>
<div class="overflow-x-auto"><table><thead><tr><th>Estudiante</th><th>Documento</th><th>Sede</th><th>Matrículas</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($students as $student)<tr wire:key="{{ $student->id }}"><td><div class="flex items-center gap-3"><span class="grid size-9 shrink-0 place-items-center rounded-full bg-slate-100 font-bold text-slate-600">{{ strtoupper(substr($student->first_name,0,1).substr($student->last_name,0,1)) }}</span><div><p class="font-semibold">{{ $student->full_name }}</p><p class="text-xs text-slate-400">{{ $student->email ?: 'Sin correo' }}</p></div></div></td><td>{{ $student->document_type }} {{ $student->document_number }}</td><td>{{ $student->campus?->name ?? '—' }}</td><td>{{ $student->enrollments_count }}</td><td><span class="badge {{ $student->active ? '' : 'muted' }}">{{ $student->active ? 'Activo' : 'Inactivo' }}</span></td><td><a href="{{ route('students.show',$student) }}" class="font-bold text-teal-700">Ver →</a></td></tr>@empty<tr><td colspan="6" class="py-12 text-center text-slate-400">No encontramos estudiantes con estos filtros.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $students->links() }}</div></div></div>
